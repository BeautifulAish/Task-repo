# Dictionary in python - "Key & value pair
# Dict purpose - store key value
# denoted by "dict"
# to use dict values should be in String format
# .get,
# If i hv duplicate key latest value is printed
# Get al the keys in to list
keys = my_dict_keys()
values = my_dict_Values()
# key list = list (keys)
print(values
      )
# Filer - Num of element is <= List

numbers = [1, 2, 3, 4, 5, 6, 7, 8]
# num %2 ==0
even_numbers = [2, 4, 6, 8]


def is_even(num):
return num % 2 == 0

output = is_even(1)
print(output)
